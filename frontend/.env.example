# ZEGO client config (non-secret — safe for the browser).
# VITE_ZEGO_APP_ID is the numeric app id; VITE_ZEGO_SERVER is the signaling
# wss:// URL. Never put the ZEGO server secret here or anywhere in the frontend;
# tokens are minted by interview-service.
VITE_ZEGO_APP_ID=
VITE_ZEGO_SERVER=